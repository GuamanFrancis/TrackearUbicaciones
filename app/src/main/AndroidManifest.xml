<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.trackearubicaciones">

    <!-- Permisos obligatorios -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" tools:targetApi="34" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Permisos adicionales -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Permiso de almacenamiento solo para versiones antiguas -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <application

        android:allowBackup="true"
        android:icon="@drawable/ic_app_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TrackearUbicaciones"
        tools:replace="android:allowBackup,android:icon,android:theme">

        <!-- Permite osmdroid guardar el caché -->
        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />

        <!-- Servicio de ubicación en primer plano -->
        <service
            android:name=".LocationForegroundService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="location"
            android:stopWithTask="false" />

        <!-- Actividades -->
        <activity
            android:name=".PerfilActivity"
            android:exported="false" />
        <activity
            android:name=".GestionarUsuariosActivity"
            android:exported="false" />
        <activity
            android:name=".UnirseSesionActivity"
            android:exported="false" />
        <activity
            android:name=".MapaColaborativoActivity"
            android:exported="false" />
        <activity
            android:name=".TopografoMainActivity"
            android:exported="false" />
        <activity
            android:name=".RegistrarTerrenoActivity"
            android:exported="false" />
        <activity
            android:name=".MisTerrenosActivity"
            android:exported="false" />
        <activity
            android:name=".AdminMainActivity"
            android:exported="false" />
        <activity
            android:name=".HomeActivity"
            android:exported="false" />
        <activity
            android:name=".RegisterActivity"
            android:exported="false" />
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>